<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BraTSInformation" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->

        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="divider">@android:color/darker_gray</item>
    </style>

    <style name="Theme.BraTSInformation.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BraTSInformation.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BraTSInformation.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>