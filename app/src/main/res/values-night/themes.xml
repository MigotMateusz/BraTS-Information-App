<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BraTSInformation" parent="Theme.MaterialComponents">
        <!-- Primary brand color. -->
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="divider">@color/divider_color_dark_mode</item>
    </style>
    <style name="Theme.BraTSInformation.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />
</resources>